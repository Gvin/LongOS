local function run()
	shell.run('/LongOS/Utilities/BiriPaint/BiriPaintImageSizeDialog.lua');
	shell.run('/LongOS/Utilities/BiriPaint/BiriPaintWindow.lua');
	local application = Classes.Application.Application('BiriPaint', false, true);
	local biriPaintWindow = BiriPaintWindow(application);
	application:Run(biriPaintWindow);
end

System:Try(run);