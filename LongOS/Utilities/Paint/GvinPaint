local function run()
	shell.run('/LongOS/Utilities/Paint/PaintWindowClass.lua');

	local application = Application('Paint', false, true);
	local paintWindow = PaintWindow(application);
	application:Run(paintWindow);
end

System:Try(run);